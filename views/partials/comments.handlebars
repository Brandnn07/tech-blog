<div class="card">
        <div class="card-content">
            {{#each this}}
          <p class="title">
              {{comment_content}}
          </p>
          <p class="subtitle">
            {{user.username}} on {{format_date date_created}}
          </p>
          {{/each}}
        </div>
      </div>